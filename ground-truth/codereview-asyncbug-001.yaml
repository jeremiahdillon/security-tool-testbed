id: codereview-asyncbug-001
category: code-review
language: javascript
title: Missing await and swallowed errors in notifier
expected_summary: Emails every task owner that their report is ready.
notes: >
  Two review-grade issues: fetchUser() returns a Promise but is not awaited, so user.email is
  undefined when passed to sendEmail; and the catch block swallows the error silently, so failed
  notifications are reported as successes. Targets CodeRabbit, not rule-based SAST.
planted:
  - type: logic-bug
    cwe: null
    severity: medium
    file: cases/code-review/js/notifier.js
    line: 8
    expected_tools: [coderabbit]
    detail: "fetchUser(id) not awaited; forEach won't await sendEmail; errors swallowed."
